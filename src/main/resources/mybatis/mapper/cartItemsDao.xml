<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.springboot.dao.ICartItemsDao"> 


		<!-- 장바구니에 추가 -->
	<insert id="cart_add" parameterType="com.study.springboot.dto.addDto"> 
		insert into shopping_cartItems(
		citems_idx, citems_title, citems_price, citems_member_idx ) 
			values (shopping_cartItems_seq.nextval, #{citems_title}, #{citems_price}, #{member_idx})
	</insert>

</mapper>