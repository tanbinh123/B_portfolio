<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.springboot.dao.INoticeDao">

<!-- 공지사항 목록 -->
<select id="notice_list">
		select * from shopping_notice order by notice_idx desc
</select>

<!-- 공지사항 보기 -->

<select id="notice_view" resultType="com.study.springboot.dto.noticeDto">
	select * from shopping_notice where notice_idx = #{param1}
</select>

<!-- 관리자전용 공지사항 작성 -->

<insert id="notice_write"  parameterType="com.study.springboot.dto.noticeDto">
	insert to shopping_notice (notice_idx, notice_title, notice_content, notice_date)
	values (shopping_notice_seq.nextval, #{param1}, #{param2}, sysdate)
</insert>

</mapper> 
